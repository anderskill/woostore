// I COULD NOT GET THIS ROUTE TO WORK AT ALL

// Warning: Did not expect server HTML to contain a <div> in <div>.

// Uncaught OptionsException {name: 'Options Error, ' message: 'consumerKey is required'}

// import { postWooCommerceOrder } from "../../utils/wooCommerceApi";
// import { customerData } from "../../mock/customerData";

const CheckoutPage = () => {
  // const newOrder = async () => {
  //   await postWooCommerceOrder(customerData);
  return (
    <div>
      <h1>
        Could not get this page to work at all, see comments in page component
      </h1>
      {/* {postWooCommerceOrder && <button onClick={newOrder}>Post order</button>} */}
    </div>
  );
};

export default CheckoutPage;
